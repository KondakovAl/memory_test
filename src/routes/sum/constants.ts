/** Время на ответ по умолчанию (мс) */
export const DEFAULT_ANSWER_MS = 15_000;

export const MIN_ANSWER_SEC = 3;
export const MAX_ANSWER_SEC = 60;

export const DEFAULT_ROUND_COUNT = 20;
export const MIN_ROUND_COUNT = 1;
export const MAX_ROUND_COUNT = 100;

/** Диапазон для слагаемых: от 1 до 99 */
export const MIN_ADDEND = 1;
export const MAX_ADDEND = 99;

/** Размер клетки с цифрой (как в memory) */
export const CELL_SIZE = 56;

/** Ширина поля ввода ответа (чтобы помещалось трёхзначное число) */
export const ANSWER_INPUT_WIDTH = 80;
